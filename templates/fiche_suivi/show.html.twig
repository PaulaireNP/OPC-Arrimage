{% extends 'base.html.twig' %}

{% block title %}FicheSuivi{% endblock %}

{% block body %}
    <h1>FicheSuivi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_suivi.id }}</td>
            </tr>
            <tr>
                <th>Rencontre</th>
                <td>{{ fiche_suivi.rencontre }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ fiche_suivi.creationDate ? fiche_suivi.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Echange</th>
                <td>{{ fiche_suivi.echange }}</td>
            </tr>
            <tr>
                <th>Orientation</th>
                <td>{{ fiche_suivi.orientation }}</td>
            </tr>
            <tr>
                <th>Echeance</th>
                <td>{{ fiche_suivi.echeance ? fiche_suivi.echeance|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fiche_suivi_index') }}">back to list</a>

    <a href="{{ path('app_fiche_suivi_edit', {'id': fiche_suivi.id}) }}">edit</a>

    {{ include('fiche_suivi/_delete_form.html.twig') }}
{% endblock %}
